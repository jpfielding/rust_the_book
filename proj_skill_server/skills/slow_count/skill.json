{
  "name": "slow_count",
  "description": "Prints 1..10, one number per second. Ignores its input.",
  "command": ["bash", "-c", "for i in $(seq 1 10); do echo $i; sleep 1; done"],
  "timeout_secs": 30
}
